# OAuth 2.0 身份认证：攻击是怎么发生的，又是怎么防住的？

> 写给零基础同学。看完这篇，你能搞懂：坏人怎么攻击、我们怎么防御、代码里长什么样。

---

## 0. 先搞懂正常流程是怎么走的

在讲攻击之前，你必须先理解**正常的登录流程**。我用一个生活化的比喻：

### 🎬 比喻：电影院取票系统

想象你去电影院看电影，流程是这样的：

```
你（用户）
  │
  ├─ 第1步：走到售票窗口，出示身份证（用户名+密码）
  │         → 售票员验证你的身份
  │
  ├─ 第2步：售票员给你一张「取票码」小纸条
  │         → 你把小纸条拿到自助取票机
  │
  ├─ 第3步：自助取票机扫描小纸条，换给你「电影票」（真正的凭证）
  │
  └─ 第4步：拿电影票进影厅、买爆米花、进VIP厅……
```

### 对应到代码里，就是这个 OAuth 2.0 授权码流程：

```
┌──────────┐     ┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  浏览器   │     │  授权服务器 :8000  │     │  客户端 :8001     │     │  资源服务器 :8002  │
│  (用户)   │     │  (售票窗口)       │     │  (取票机)         │     │  (影厅检票口)     │
└──────────┘     └──────────────────┘     └──────────────────┘     └──────────────────┘
     │                    │                       │                       │
     │ ①访问/login        │                       │                       │
     │───────────────────▶│                       │                       │
     │                    │                       │                       │
     │ ②弹出登录页         │                       │                       │
     │◀───────────────────│                       │                       │
     │                    │                       │                       │
     │ ③输入alice/alice123│                       │                       │
     │───────────────────▶│                       │                       │
     │                    │                       │                       │
     │ ④302重定向          │                       │                       │
     │  带上code=xxxxx    │                       │                       │
     │◀───────────────────│                       │                       │
     │                    │                       │                       │
     │ ⑤浏览器自动跳转到/callback?code=xxxxx       │                       │
     │───────────────────────────────────────────▶│                       │
     │                    │                       │                       │
     │                    │ ⑥用code换token         │                       │
     │                    │◀──────────────────────│                       │
     │                    │                       │                       │
     │                    │ ⑦返回access_token     │                       │
     │                    │──────────────────────▶│                       │
     │                    │                       │                       │
     │                    │                       │ ⑧拿token访问/profile  │
     │                    │                       │──────────────────────▶│
     │                    │                       │                       │
     │                    │                       │ ⑨返回用户信息          │
     │                    │                       │◀──────────────────────│
     │                    │                       │                       │
     │ ⑩显示"登录成功，欢迎 alice"                  │                       │
     │◀───────────────────────────────────────────│                       │
```

**关键对应关系：**

| 电影院比喻 | 代码里的术语 | 解释 |
|-----------|------------|------|
| 售票窗口 | Authorization Server (:8000) | 验证你是谁，发授权码 |
| 取票码小纸条 | `authorization_code` | 一次性凭证，用完即废 |
| 自助取票机 | Client App `/token` | 用取票码换真正的凭证 |
| 电影票 | `access_token` (JWT) | 真正的凭证，30分钟有效 |
| 影厅检票口 | Resource Server (:8002) | 查验你的票，决定你能去哪 |

**这段流程在你代码里的位置：**

| 步骤 | 文件 | 函数 |
|------|------|------|
| ①②③ | `auth_server/routes.py` | `authorize_page()` / `authorize_submit()` |
| ④ | `auth_server/routes.py` 第108-109行 | 302 重定向 |
| ⑤⑥⑦ | `client_app/routes.py` | `callback()` 第118-198行 |
| ⑧⑨ | `resource_server/routes.py` | `profile()` / `email()` / `admin()` |

---

## ⚔️ 攻击一：授权码截获攻击

### 攻击原理（一句话）

> 坏人偷看你手里的小纸条（取票码），然后自己去取票机换了张票。

### 图解攻击过程

```
正常用户 Alice                          坏蛋 Mallory
     │                                      │
     │ ①正常登录，输入 alice/alice123         │
     │──────────────────────────────→        │
     │         授权服务器                     │
     │                                      │
     │ ②302重定向：/callback?code=ABC123     │
     │←──────────────────────────────        │
     │                                      │
     │         ⚠️ 重定向URL经过网络           │
     │         坏蛋在中间截获了这个URL！        │
     │                                      │
     │                             ③看到 code=ABC123！
     │                               直接拿着去换token
     │                                      │
     │                             ④POST /token
     │                                code=ABC123
     │                                ──────────────→ 授权服务器
     │                                      │
     │                             ⑤返回 access_token！
     │                              ←──────────────
     │                                      │
     │                             ⑥拿token访问 /profile
     │                                ──────────────→ 资源服务器
     │                                      │
     │                             ⑦返回Alice的个人信息
     │                              ←──────────────
     │                                      │
     │                          🔴 攻击成功！
     │                      坏蛋冒充了Alice的身份
```

### 为什么能成功？

因为 OAuth 2.0 原始规范中，`authorization_code` 就像一张**不记名支票**——谁拿着它都能去兑换。授权服务器不检查"来兑票的人是不是当初拿取票码的那个人"。

**对应代码**：`auth_server/routes.py` 第112-137行，`/token` 接口只验证：
- code 是否存在 ✅
- code 有没有过期 ✅
- code 有没有被用过 ✅

但它**不验证**"是不是合法客户端在兑换"——**没有 PKCE 的时候**，任何人都能换。

### 实验结果（你的代码跑出来的）

从 `attack_simulator/results/latest_summary.csv`：

```
experiment: authorization_code_interception
defense: no_pkce
attack_success: True          ← 🔴 攻击成功！
token_status: 200             ← 换到了token
resource_status: 200          ← 访问了Alice的资料
```

### 🛡️ 防御方案：PKCE（Proof Key for Code Exchange）

PKCE 的防法很简单：**在拿取票码的时候，你提前告诉售票员一个"暗号"，取票的时候必须对上暗号才行。**

```
原来（无PKCE）：
  你：给我取票码
  售票员：给你 ABC123
  
  坏蛋：我是来取票的，取票码是 ABC123
  售票员：好的，给你票    ← 🔴 任何人都能换！

现在（有PKCE）：
  你：给我取票码，暗号是 [SHA256(我的密语)] = "X7k9..."
  售票员：好的，取票码 ABC123，我记下你的暗号了
  
  你：我来取票，取票码 ABC123，密语原文是 "my-secret"
  售票员：SHA256("my-secret") = "X7k9..." ✅ 暗号对上，给你票
  
  坏蛋：我来取票，取票码 ABC123 （不知道密语原文）
  售票员：你的暗号呢？（我存的是X7k9...）
  坏蛋：什么暗号？随便编一个 "Y8k0..."
  售票员：SHA256("Y8k0...") ≠ "X7k9..." ❌ 暗号不对，滚！
```

**关键点**：
- 授权时传的是 **哈希值**（`code_challenge`），不是原文
- 换token时传的是 **原文**（`code_verifier`）
- 服务器做 `SHA256(原文) == 哈希值？` 的验证
- 坏蛋即使截获了哈希值，也无法反推出原文（SHA-256 不可逆）

**对应代码**：

授权时，客户端生成 PKCE 参数（`client_app/services.py` 第9-37行）：
```python
# 生成随机密语
verifier = secrets.token_urlsafe(48)
# 计算哈希
digest = hashlib.sha256(verifier.encode()).digest()
challenge = base64.urlsafe_b64encode(digest).decode().rstrip("=")
# 带着 challenge 去授权
params["code_challenge"] = challenge
params["code_challenge_method"] = "S256"
```

换token时，服务器验证（`auth_server/routes.py` 第139-147行）：
```python
if code_record.code_challenge:          # 如果授权时设置了暗号
    if not code_verifier:               # 换token时必须带密语
        raise HTTPException(400, "invalid_code_verifier")
    if code_record.code_challenge_method == "S256":
        expected = SHA256(code_verifier) # 计算密语的哈希
    if expected != code_record.code_challenge:
        raise HTTPException(400, "invalid_code_verifier")  # 对不上！
```

### 实验结果（有PKCE时）

```
experiment: authorization_code_interception
defense: pkce_s256
attack_success: False         ← ✅ 防御成功！
token_status: 400             ← 换token失败！
reason: "PKCE blocks the intercepted code because 
         the attacker lacks the code_verifier"
```

---

## ⚔️ 攻击二：登录 CSRF 攻击

### 攻击原理（一句话）

> 坏蛋自己先去取一张票，然后骗你"这是你的票"，让你不知不觉用了坏蛋的身份。

### 图解攻击过程

这比第一个攻击稍微绕一点，我用场景说明：

**正常流程**：
```
Alice 点击"登录" → 浏览器保存 state cookie → 授权后回调带 state
→ 对比 cookie 里的 state 和 URL 里的 state → 一致，放行
```

**坏蛋的 CSRF 攻击**：
```
① 坏蛋自己先正常登录，拿到了自己的 code = "MALLORY_CODE"
   同时拿到了 state = "MALLORY_STATE"

② 坏蛋构造一个恶意链接：
   http://127.0.0.1:8001/callback?code=MALLORY_CODE&state=MALLORY_STATE

③ 坏蛋把这个链接发给 Alice（伪装成"点我领优惠券"之类的）

④ Alice 点击了这个链接 → 浏览器带着 Alice 自己的 state cookie
   访问 /callback?code=MALLORY_CODE&state=MALLORY_STATE

⑤ 如果客户端不做 state 校验：
   "哦，code=MALLORY_CODE，state=MALLORY_STATE，都是有效的，放行"
   
   🔴 结果：Alice 的浏览器会话里，登录的却是坏蛋的身份！
   之后 Alice 在网站上做的操作，都是用的坏蛋的账号。
```

### 为什么危险？

- Alice 可能不知不觉地把自己的个人信息填到了坏蛋的账号里
- 或者反过来：坏蛋构造一个"用自己的 code 但等待 Alice 输入敏感信息"的陷阱
- 本质是 **把两个不同的人的错误会话绑定到了一起**

### 🛡️ 防御方案：State 参数绑定

防御逻辑极其简单：**你登录时我在你浏览器里放一个"暗号 cookie"，回调时你必须把这个暗号原样带回来。**

```
登录时（/login）：
  生成随机 state = "aB3xK9..."
  写进 HttpOnly cookie：oauth_state = "aB3xK9..."
  写进授权URL：?state=aB3xK9...
  
回调时（/callback）：
  读取 cookie 里的 oauth_state = "aB3xK9..."
  读取 URL 里的 ?state=aB3xK9...
  
  if cookie里没有 state → 拒绝："missing saved state"
  if URL里没有 state   → 拒绝："missing state"  
  if cookie ≠ URL      → 拒绝："state mismatch"
  if cookie == URL      → 放行 ✅
```

**对应代码**：`client_app/routes.py` 第119-126行：

```python
saved_state = request.cookies.get("oauth_state", "")  # 从cookie读
if not saved_state:       # cookie里没有 → 拦
    raise HTTPException(400, "missing saved state")
if not state:             # URL里没有 → 拦
    raise HTTPException(400, "missing state")
if saved_state != state:  # 对不上 → 拦
    raise HTTPException(400, "state mismatch")
```

**为什么坏蛋绕不过去？**

坏蛋能控制 URL 里的 state（他可以随便写），但他**无法修改 Alice 浏览器里的 cookie**（因为 cookie 是 HttpOnly 的，JS 读不到；而且 cookie 是 Alice 第一次点"登录"时服务器写的，坏蛋改不了）。

### 实验结果

```
三种变体全部被拦：
- 缺cookie里的state → 400 "missing saved state"
- 缺URL里的state    → 400 "missing state"
- state不匹配       → 400 "state mismatch"

attack_success: False ✅ 全部防御成功
```

---

## ⚔️ 攻击三：Scope 权限滥用攻击

### 攻击原理（一句话）

> 普通观众拿着普通票，非要硬闯 VIP 厅——就看检票口拦不拦。

### 图解攻击过程

系统里有两个用户：

| 用户名 | 密码 | 角色 | 允许的权限（scope） |
|--------|------|------|-------------------|
| alice | alice123 | user（普通用户） | `read:profile` `read:email` |
| admin | admin123 | admin（管理员） | `read:profile` `read:email` `admin:panel` |

Alice 的"权限清单"里没有 `admin:panel`，但如果她**故意在授权请求里加上**呢？

```
Alice 正常登录时，本来应该请求：
  GET /authorize?scope=read:profile read:email

但 Alice（或者被篡改的客户端）请求：
  GET /authorize?scope=read:profile read:email admin:panel
                                               ^^^^^^^^^^
                                               我明明没有这个权限，
                                               但我就硬要！
```

### 结果：两道防线让攻击失败

**第一道防线 — 授权服务器裁剪 scope**：

`auth_server/routes.py` 第33-36行的 `_clip_scope()` 函数：

```python
def _clip_scope(requested_scope, allowed_scope):
    allowed = set(allowed_scope.split())          # Alice的：{read:profile, read:email}
    granted = [s for s in requested_scope.split() if s in allowed]  # 取交集！
    return " ".join(granted)
    # Alice请求了：read:profile, read:email, admin:panel
    # 交集结果是： read:profile, read:email        ← admin:panel 被砍掉了！
```

所以 Alice 拿到的 token 里，scope 字段只有 `read:profile read:email`，**不包含 `admin:panel`**。

**第二道防线 — 资源服务器双重校验**：

`resource_server/routes.py` 第47-51行的 `/admin` 接口：

```python
@app.get("/admin")
def admin(payload):
    require_scope(payload["scope"], "admin:panel")  # ① scope里必须有admin:panel
    require_role(payload["role"], "admin")           # ② 角色必须是admin
    # Alice 两个条件都不满足 → 403
```

即使第一道防线出了bug，第二道防线也能拦下。这就是**纵深防御**（Defense in Depth）。

### 实验结果

```
alice 请求 admin:panel：
  token_status: 200         ← token还是签发了（只是scope被裁剪）
  /admin status: 403        ← 🔒 访问被拒！"missing scope: admin:panel"
  attack_success: False ✅

admin 请求 admin:panel：
  token_status: 200         ← token正常签发
  /admin status: 200        ← 正常访问
  granted_scope: read:profile read:email admin:panel
```

---

## 🛡️ 总结：三层防御体系一览

```
┌─────────────────────────────────────────────────────────┐
│                    你的系统 = 三层防线                     │
├──────────────┬─────────────────┬─────────────────────────┤
│   防线       │  机制            │  防什么                  │
├──────────────┼─────────────────┼─────────────────────────┤
│  第一层      │  PKCE (S256)     │  授权码截获              │
│              │  code_challenge  │  坏蛋偷了你的code        │
│              │  ↔ code_verifier │  也换不了token          │
├──────────────┼─────────────────┼─────────────────────────┤
│  第二层      │  State 参数绑定   │  登录 CSRF              │
│              │  cookie ↔ URL    │  坏蛋不能把两个错误的    │
│              │  必须一致         │  会话绑在一起            │
├──────────────┼─────────────────┼─────────────────────────┤
│  第三层      │  Scope 裁剪       │  权限提升               │
│              │  + Role/Scope    │  普通用户拿不到          │
│              │  双重校验        │  管理员权限              │
└──────────────┴─────────────────┴─────────────────────────┘

三层全开 → 攻击成功率 = 0/5 = 0% ✅
```

### 代码里的开关

所有配置在 `config/settings.py`：
```python
ENABLE_PKCE = False        # ← 当前默认关闭！改成True就启用第一道防线
```

### 自己动手跑实验

```bash
# 1. 先启动三个服务
python scripts/run_all.py

# 2. 跑C同学的攻击实验
python scripts/run_c_experiments.py

# 3. 查看结果
cat attack_simulator/results/latest_summary.csv
```

---

## 🎯 一句话记住

| 攻击 | 为什么能成功 | 怎么防 | 代码在哪里 |
|------|------------|--------|-----------|
| 授权码截获 | code 是不记名凭证，谁拿到都能换 | PKCE：换token时验"暗号" | `auth_server/routes.py:139-147` |
| 登录CSRF | 坏蛋可以伪造回调链接 | State：cookie和URL必须对上 | `client_app/routes.py:120-126` |
| Scope滥用 | 客户端可以乱要权限 | 裁剪：服务器只给用户该有的 | `auth_server/routes.py:33-36` + `resource_server/routes.py:48-51` |

---

> 写于 2026年 — 基于 `identity_final` 项目代码和 C 同学的攻击实验数据
